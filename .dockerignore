# Never ship secrets or local state into the image.
.env
.env.*
!.env.example

node_modules
npm-debug.log*
.git
.gitignore
*.md
.DS_Store

# Local user store from the pre-Firebase build, if any lingers.
src/data/users.json
